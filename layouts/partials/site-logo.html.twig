{% set logo = asset(site.logo) %}
        <a class="logo" href="{{ url(site.home) }}">
          <img src="{{ logo }}" width="{{ logo.width }}" height="{{ logo.height }}" alt="{{ site.title }} logo"/>
        </a>
