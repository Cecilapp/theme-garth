{% set logo = asset(site.logo) %}
        <a class="logo" href="{{ url(site.home) }}">
          <img src="{{ logo|dataurl }}" width="{{ logo.width }}" width="{{ logo.height }}" alt="{{ site.title }} logo" />
        </a>
